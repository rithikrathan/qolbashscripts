proj_path=$(find "$HOME/Desktop/projects" -mindepth 1 -maxdepth 1 -type d | rofi -dmenu -p "Select project" -matching fuzzy)
[ -z "$proj_path" ] && exit 
gnome-terminal --working-directory="$proj_path"
